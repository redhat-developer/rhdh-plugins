import path from 'path';
const exportRules = (rulesInput) => {
    return rulesInput.reduce((rulesOutput, rule) => (Object.assign(Object.assign({}, rulesOutput), { [rule]: require(`./${path.join('./rules/', rule)}`).default })), {});
};
module.exports = {
    rules: exportRules(['best-practices/no-discouraged-words']),
};
//# sourceMappingURL=index.js.map