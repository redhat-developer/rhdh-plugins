import type { Compiler, Compilation, WebpackPluginInstance, Module } from 'webpack';
import { moduleFederationPlugin } from '@module-federation/sdk';
/**
 * This class is used to hoist container references in the code.
 * @constructor
 */
export declare class HoistContainerReferences implements WebpackPluginInstance {
    private readonly containerName;
    private readonly entryFilePath?;
    private readonly bundlerRuntimeDep?;
    private readonly explanation;
    private readonly experiments;
    constructor(name?: string, entryFilePath?: string, bundlerRuntimeDep?: string, experiments?: moduleFederationPlugin.ModuleFederationPluginOptions['experiments']);
    apply(compiler: Compiler): void;
    private findModule;
    private hoistModulesInChunks;
    private cleanUpChunks;
    private getRuntimeChunks;
}
export declare function getAllReferencedModules(compilation: Compilation, module: Module, type?: 'all' | 'initial'): Set<Module>;
export default HoistContainerReferences;
