# yaml-language-server: $schema=../../json-schema/plugins.json
apiVersion: extensions.backstage.io/v1alpha1
kind: Plugin
metadata:
  namespace: extensions-plugin-demo
  name: extensions
  title: Extensions
  description: The Extensions plugin allows you to discover and install plugins in Backstage.
  tags:
    - extensions
    - catalog
  annotations:
    extensions.backstage.io/pre-installed: 'true'
spec:
  author: Backstage Community
  categories:
    - Catalog
  support:
    provider: Red Hat
    level: dev-preview
  lifecycle: production
  description: todo
  installation: todo
  packages:
    # for testing purposes, we are using two different package references here
    - extensions-plugin-demo/red-hat-developer-hub-backstage-plugin-extensions
    - package:extensions-plugin-demo/red-hat-developer-hub-backstage-plugin-extensions-backend
