# yaml-language-server: $schema=../../json-schema/plugins.json
apiVersion: extensions.backstage.io/v1alpha1
kind: Plugin
metadata:
  namespace: extensions-plugin-demo
  name: pre-installed-true
  title: Pre-installed True
  description: Pre-installed true means that the YAML is bundled with RHDH and this means this is NOT a custom plugin.
  annotations:
    extensions.backstage.io/pre-installed: 'true'
spec:
  categories:
    - Test plugin entity
