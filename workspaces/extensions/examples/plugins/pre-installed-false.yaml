# yaml-language-server: $schema=../../json-schema/plugins.json
apiVersion: extensions.backstage.io/v1alpha1
kind: Plugin
metadata:
  namespace: extensions-plugin-demo
  name: pre-installed-false
  title: Pre-installed False
  description: Pre-installed false means that the YAML is NOT bundled with RHDH and this means this IS a custom plugin.
  annotations:
    extensions.backstage.io/pre-installed: 'false'
spec:
  categories:
    - Test plugin entity
