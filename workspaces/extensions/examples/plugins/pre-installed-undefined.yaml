# yaml-language-server: $schema=../../json-schema/plugins.json
apiVersion: extensions.backstage.io/v1alpha1
kind: Plugin
metadata:
  namespace: extensions-plugin-demo
  name: pre-installed-undefined
  title: Pre-installed Undefined
  description: Pre-installed undefined means that the YAML is NOT bundled with RHDH and this means this IS a custom plugin.
spec:
  categories:
    - Test plugin entity
