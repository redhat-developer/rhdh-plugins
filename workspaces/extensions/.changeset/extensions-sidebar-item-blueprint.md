---
'@red-hat-developer-hub/backstage-plugin-extensions': patch
---

Register the Extensions nav entry through `SidebarItemBlueprint` in the `admin` group, and adopt RHDH app-defaults for the NFS demo app sidebar instead of a custom nav module.
