---
'@red-hat-developer-hub/backstage-plugin-intelligent-assistant-backend': minor
---

Accept WebP (`image/webp`) image attachments on the `/v1/query` route alongside the existing JPEG support, validating them by their `RIFF....WEBP` magic bytes.
