---
'@red-hat-developer-hub/backstage-plugin-scorecard-backend': minor
---

Add `scorecard.entityAnnotations.thresholds` (and global `entityAnnotations.enabled`) so administrators can disable or restrict per-entity threshold override annotations, using the same pattern as disabled-metrics annotation controls.
