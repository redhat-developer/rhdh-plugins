---
'@red-hat-developer-hub/backstage-plugin-scorecard': minor
---

Add sparkline charts on the entity Scorecard tab for metrics whose `defaultVisualization` is `sparkline` (for example DORA). Each card shows a 30-day trend, a threshold legend, and a View data sources dialog for collectors. Legend and line color come from the time-series threshold rules.
