---
'@red-hat-developer-hub/backstage-plugin-orchestrator': patch
---

Add an Orchestrator sidebar item via `SidebarItemBlueprint` and rely on app-defaults navigation instead of a custom nav module in the NFS app and plugin playground.
