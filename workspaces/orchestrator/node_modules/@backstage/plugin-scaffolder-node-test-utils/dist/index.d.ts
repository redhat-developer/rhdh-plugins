import { JsonObject } from '@backstage/types';
import { ActionContext } from '@backstage/plugin-scaffolder-node';

/**
 * A utility method to create a mock action context for scaffolder actions.
 *
 * @public
 * @param options - optional parameters to override default mock context
 */
declare function createMockActionContext<TActionInput extends JsonObject = JsonObject, TActionOutput extends JsonObject = any>(options?: Partial<ActionContext<TActionInput, TActionOutput>>): ActionContext<TActionInput, TActionOutput>;

export { createMockActionContext };
