'use strict';

var paths = require('../../../lib/paths.cjs.js');

function tryRunPrettier(content, extraConfig = { parser: "markdown" }) {
  try {
    const prettier = require("prettier");
    const config = prettier.resolveConfig.sync(paths.paths.targetRoot) ?? {};
    return prettier.format(content, {
      ...config,
      ...extraConfig
    });
  } catch (e) {
    return content;
  }
}

exports.tryRunPrettier = tryRunPrettier;
//# sourceMappingURL=tryRunPrettier.cjs.js.map
