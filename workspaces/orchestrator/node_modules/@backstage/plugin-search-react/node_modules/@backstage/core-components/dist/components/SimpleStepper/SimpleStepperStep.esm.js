import { jsxs, jsx } from 'react/jsx-runtime';
import Box from '@material-ui/core/Box';
import MuiStep from '@material-ui/core/Step';
import StepContent from '@material-ui/core/StepContent';
import StepLabel from '@material-ui/core/StepLabel';
import { makeStyles } from '@material-ui/core/styles';
import Typography from '@material-ui/core/Typography';
import { SimpleStepperFooter } from './SimpleStepperFooter.esm.js';

const useStyles = makeStyles(
  (theme) => ({
    end: {
      padding: theme.spacing(3)
    }
  }),
  { name: "SimpleStepperStep" }
);
function SimpleStepperStep(props) {
  const { title, children, end, actions, ...muiProps } = props;
  const classes = useStyles();
  return end ? /* @__PURE__ */ jsxs(Box, { className: classes.end, children: [
    /* @__PURE__ */ jsx(Typography, { variant: "h6", children: title }),
    children,
    /* @__PURE__ */ jsx(SimpleStepperFooter, { actions: { ...actions || {}, showNext: false } })
  ] }) : /* @__PURE__ */ jsxs(MuiStep, { ...muiProps, children: [
    /* @__PURE__ */ jsx(StepLabel, { children: /* @__PURE__ */ jsx(Typography, { variant: "h6", children: title }) }),
    /* @__PURE__ */ jsxs(StepContent, { children: [
      children,
      /* @__PURE__ */ jsx(SimpleStepperFooter, { actions })
    ] })
  ] });
}

export { SimpleStepperStep };
//# sourceMappingURL=SimpleStepperStep.esm.js.map
