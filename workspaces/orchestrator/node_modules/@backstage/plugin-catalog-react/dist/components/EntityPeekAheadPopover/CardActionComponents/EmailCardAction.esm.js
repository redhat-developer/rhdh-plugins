import { jsx } from 'react/jsx-runtime';
import IconButton from '@material-ui/core/IconButton';
import EmailIcon from '@material-ui/icons/Email';
import { Link } from '@backstage/core-components';
import { catalogReactTranslationRef } from '../../../translation.esm.js';
import { useTranslationRef } from '@backstage/core-plugin-api/alpha';

const EmailCardAction = (props) => {
  const { t } = useTranslationRef(catalogReactTranslationRef);
  return /* @__PURE__ */ jsx(
    IconButton,
    {
      component: Link,
      "aria-label": "Email",
      title: t("entityPeekAheadPopover.emailCardAction.title", {
        email: props.email
      }),
      to: t("entityPeekAheadPopover.emailCardAction.subTitle", {
        email: props.email
      }),
      children: /* @__PURE__ */ jsx(EmailIcon, {})
    }
  );
};

export { EmailCardAction };
//# sourceMappingURL=EmailCardAction.esm.js.map
