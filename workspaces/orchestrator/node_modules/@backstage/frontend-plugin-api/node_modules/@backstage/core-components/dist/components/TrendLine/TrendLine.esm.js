import { jsxs, jsx } from 'react/jsx-runtime';
import { Sparklines, SparklinesLine } from 'react-sparklines';
import { useTheme } from '@material-ui/core/styles';

function color(data, theme) {
  const lastNum = data[data.length - 1];
  if (!lastNum) return void 0;
  if (lastNum >= 0.9) return theme.palette.status.ok;
  if (lastNum >= 0.5) return theme.palette.status.warning;
  return theme.palette.status.error;
}
function TrendLine(props) {
  const theme = useTheme();
  if (!props.data) return null;
  return /* @__PURE__ */ jsxs(Sparklines, { width: 120, height: 30, min: 0, max: 1, ...props, children: [
    props.title && /* @__PURE__ */ jsx("title", { children: props.title }),
    /* @__PURE__ */ jsx(SparklinesLine, { color: props.color ?? color(props.data, theme) })
  ] });
}

export { TrendLine };
//# sourceMappingURL=TrendLine.esm.js.map
