import { palettes } from '@backstage/theme';

const getDefaultThemeConfig = (mode) => {
  const palette = mode === "dark" ? palettes.dark : palettes.light;
  return {
    variant: "backstage",
    mode: mode === "dark" ? "dark" : "light",
    palette
  };
};

export { getDefaultThemeConfig };
//# sourceMappingURL=backstage.esm.js.map
