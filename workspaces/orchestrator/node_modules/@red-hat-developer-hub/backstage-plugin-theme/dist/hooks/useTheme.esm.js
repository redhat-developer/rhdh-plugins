import { useMemo } from 'react';
import { createUnifiedTheme } from '@backstage/theme';
import { useThemeOptions } from './useThemeOptions.esm.js';

const useTheme = (themeConfig) => {
  const unifiedThemeOptions = useThemeOptions(themeConfig);
  const theme = useMemo(
    () => createUnifiedTheme(unifiedThemeOptions),
    [unifiedThemeOptions]
  );
  return theme;
};

export { useTheme };
//# sourceMappingURL=useTheme.esm.js.map
