import { useMemo } from 'react';
import { useApi, configApiRef } from '@backstage/core-plugin-api';

const useBranding = () => {
  let configApi = undefined;
  try {
    configApi = useApi(configApiRef);
  } catch (err) {
  }
  return useMemo(() => {
    const branding = configApi?.getOptional("app.branding");
    return branding;
  }, [configApi]);
};

export { useBranding };
//# sourceMappingURL=useBranding.esm.js.map
