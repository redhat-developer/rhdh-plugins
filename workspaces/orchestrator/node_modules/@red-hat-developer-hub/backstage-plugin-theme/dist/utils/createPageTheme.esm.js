import { shapes, genPageTheme } from '@backstage/theme';

const createPageTheme = (pageThemeConfig) => {
  let colors = pageThemeConfig.backgroundColor ?? pageThemeConfig.colors;
  if (typeof colors === "string") {
    colors = [colors];
  } else if (!colors?.length) {
    colors = ["transparent"];
  }
  let shape = pageThemeConfig.backgroundImage ?? shapes[pageThemeConfig.shape] ?? pageThemeConfig.shape ?? "none";
  if (shape.startsWith("data:") || shape.startsWith("http://") || shape.startsWith("https://") || shape.startsWith("/")) {
    shape = `url("${shape}")`;
  } else if (shape.startsWith("<svg") && shape.endsWith(">")) {
    shape = `url("data:image/svg+xml,${encodeURIComponent(shape)}")`;
  }
  return genPageTheme({
    colors,
    shape,
    options: {
      fontColor: pageThemeConfig.fontColor
      // default is white in genPageTheme!
    }
  });
};

export { createPageTheme };
//# sourceMappingURL=createPageTheme.esm.js.map
