'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var fs = require('fs-extra');
var paths = require('../../lib/paths.cjs.js');
var collect = require('../../lib/schema/collect.cjs.js');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var fs__default = /*#__PURE__*/_interopDefaultCompat(fs);

var command = async () => {
  const { name } = await fs__default.default.readJson(paths.paths.resolveTarget("package.json"));
  const configSchema = await collect.getConfigSchema(name);
  process.stdout.write(`${JSON.stringify(configSchema, null, 2)}
`);
};

exports.default = command;
//# sourceMappingURL=command.cjs.js.map
