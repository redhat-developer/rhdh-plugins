'use strict';

var bundlePlugin = require('../bundler/bundlePlugin.cjs.js');
var parallel = require('../parallel.cjs.js');

async function buildScalprumPlugin(options) {
  const { targetDir, pluginMetadata, resolvedScalprumDistPath } = options;
  await bundlePlugin.buildScalprumBundle({
    targetDir,
    entry: "src/index",
    parallelism: parallel.getEnvironmentParallelism(),
    pluginMetadata,
    resolvedScalprumDistPath
  });
}

exports.buildScalprumPlugin = buildScalprumPlugin;
//# sourceMappingURL=buildScalprumPlugin.cjs.js.map
