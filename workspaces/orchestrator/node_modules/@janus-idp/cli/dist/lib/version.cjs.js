'use strict';

var fs = require('fs-extra');
require('semver');
var _package = require('../package.json.cjs.js');
var paths = require('./paths.cjs.js');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var fs__default = /*#__PURE__*/_interopDefaultCompat(fs);

function findVersion() {
  const pkgContent = fs__default.default.readFileSync(paths.paths.resolveOwn("package.json"), "utf8");
  return JSON.parse(pkgContent).version;
}
const version = findVersion();
({
  ...Object.fromEntries(
    Object.entries(_package.default.devDependencies).filter(
      ([k, _v]) => k.startsWith("@backstage/")
    )
  ),
  ...Object.fromEntries(
    Object.entries(_package.default.dependencies).filter(
      ([k, _v]) => k.startsWith("@backstage/")
    )
  ),
  "@janus-idp/cli": version
});

exports.findVersion = findVersion;
exports.version = version;
//# sourceMappingURL=version.cjs.js.map
