'use strict';

var React = require('react');
var lodash = require('lodash');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefaultCompat(React);

const useDeepCompareMemoize = (value, stringify) => {
  const ref = React__default.default.useRef();
  if (stringify ? JSON.stringify(value) !== JSON.stringify(ref.current) : !lodash.isEqual(value, ref.current)) {
    ref.current = value;
  }
  return ref.current;
};

exports.useDeepCompareMemoize = useDeepCompareMemoize;
//# sourceMappingURL=useDeepCompareMemoize.cjs.js.map
