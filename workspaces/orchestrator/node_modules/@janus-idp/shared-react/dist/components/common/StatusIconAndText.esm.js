import React from 'react';
import cx from 'classnames';
import { CamelCaseWrap } from './CamelCaseWrap.esm.js';
import './StatusIconAndText.css.esm.js';

const DASH = "-";
const StatusIconAndText = ({
  icon,
  title,
  spin,
  iconOnly,
  className,
  dataTestId
}) => {
  if (!title) {
    return /* @__PURE__ */ React.createElement(React.Fragment, null, DASH);
  }
  if (iconOnly) {
    return /* @__PURE__ */ React.createElement(React.Fragment, null, React.cloneElement(icon, {
      "data-testid": dataTestId ?? `icon-only-${title}`,
      className: icon.props.className
    }));
  }
  return /* @__PURE__ */ React.createElement(
    "span",
    {
      className: cx("bs-shared-icon-and-text", className),
      "data-testid": dataTestId ?? `icon-with-title-${title}`,
      title
    },
    React.cloneElement(icon, {
      className: cx(
        spin && "fa-spin",
        icon.props.className,
        "bs-shared-icon-and-text__icon bs-shared-icon-flex-child"
      )
    }),
    /* @__PURE__ */ React.createElement(CamelCaseWrap, { value: title, dataTest: "status-text" })
  );
};

export { StatusIconAndText, StatusIconAndText as default };
//# sourceMappingURL=StatusIconAndText.esm.js.map
