---
'@red-hat-developer-hub/backstage-plugin-lightspeed': patch
---

Lightspeed shell (overlay and docked) now includes **Chat** and **Notebooks** tabs so you can switch views without leaving the panel.

- **Notebooks tab**: Chat-only header options (for example pinned chats and MCP settings) are hidden while Notebooks is active.
- **Display mode changes**: The selected tab is preserved when switching between overlay, docked, and fullscreen, and entering fullscreen from Notebooks navigates straight to the notebooks route so the URL and tab no longer jump through Chat first.
