---
'@red-hat-developer-hub/backstage-plugin-quickstart': minor
---

Graduated NFS plugin APIs from alpha to stable. The `quickstartInitModule`, `quickstartTranslationsModule`, and the default NFS plugin export are now available from the main package entry point. The `/alpha` subpath is deprecated but remains available for backward compatibility.
