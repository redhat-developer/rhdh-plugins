node_modules/
build/
coverage/
*.log
.yarn/

# Re-include the dist/ directory (root .gitignore excludes 'dist'). Only the
# committed bundle should be tracked; anything else under dist/ stays ignored.
!dist/
dist/*
!dist/install-dynamic-plugins.cjs
