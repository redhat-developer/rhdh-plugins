---
'@red-hat-developer-hub/backstage-plugin-global-floating-action-button': patch
---

Replace JSS `makeStyles` with MUI `sx` props to prevent CSS class name collisions with other dynamically loaded plugins in RHDH.
