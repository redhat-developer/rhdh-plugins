# DCM Backend

Full Backstage backend for local development. Runs catalog, auth, scaffolder, techdocs, search, and the DCM backend plugin.

Start with `yarn start` from the workspace root (runs app + backend via `yarn start`), or `yarn workspace backend start` from the dcm workspace.
