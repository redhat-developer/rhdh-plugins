---
'@red-hat-developer-hub/backstage-plugin-theme': minor
---

Graduate the New Frontend System (NFS) theme module from the `./alpha` export to the primary `./` entry point. OFS (legacy) exports are now available at `./legacy`.
