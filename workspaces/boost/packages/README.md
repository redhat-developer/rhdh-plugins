# The Packages Folder

This is where your own applications and centrally managed libraries live, each
in a separate folder of its own.

The `app` folder hosts the New Frontend System dev app; the `backend` folder
hosts the Backstage backend. Both are wired with all registerable Boost plugins
for full-stack local development via `yarn start` from the workspace root.
