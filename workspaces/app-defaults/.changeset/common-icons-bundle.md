---
'@red-hat-developer-hub/backstage-plugin-app-defaults': minor
---

Register the RHDH common icon catalog on `appDefaultsModule` via `IconBundleBlueprint`, so NFS apps get the same named icons as the legacy shell without wiring them in `packages/app-next`.
